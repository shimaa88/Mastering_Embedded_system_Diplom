
#include <stdio.h>
#include "string.h"
#define SIZE   50
int main(void) {
char st[SIZE];

int i;
printf("Enter a string:");
fflush(stdout);
gets(st);

while(st[i]!=0)
{
	i++;
}
printf("Length of string :%d",i);
fflush(stdout);
}

